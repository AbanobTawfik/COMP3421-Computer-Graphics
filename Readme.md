To run this code open a git bash at each assignment folder and type the following command:
- ./fetch_deps.sh -G Ninja
Now you can open the entire assignment folders as a clion project and run the code! :D